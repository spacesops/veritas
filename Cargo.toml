[package]
name = "vertias-app-core"
version = "0.1.4"
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/spacesprotocol/veritas"
homepage = "https://spacesprotocol.org"
authors = ["Buffrr <contact@buffrr.dev>"]
publish = false

[lib]
path = "core/src/lib.rs"
crate-type = ["staticlib", "lib"]

[[bin]]
name = "veritas"
path = "core/bin/veritas.rs"

[[bin]]
name = "uniffi-bindgen"
path = "core/bin/uniffi_bindgen.rs"

[dependencies]
tokio = { version = "1.37", features = ["sync", "rt-multi-thread", "macros"] }
spaces_client = "0.5"
spaces_nums = { version = "0.5", features = ["bech32"] }
spaces_protocol = { version = "0.5", features = ["std"] }
spaces_checkpoint = "0.5"
fabric = { package = "fabric-resolver", version = "0.2.11" }
yuki = { package = "bitcoin_yuki", version = "0.0.1" }
anyhow = "1.0.102"
tracing = "0.1.44"
tracing-subscriber = { version = "0.3", features = ["tracing-log"] }
dirs = "6"
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
hex = "0.4"
base64 = "0.22"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }
hyper = { version = "0.14", features = ["server", "http1", "tcp", "client", "runtime"] }
uniffi = { version = "0.31", features = ["cli", "tokio"] }
clap = { version = "4", features = ["derive"] }
tokio-tungstenite = { version = "0.26", features = ["rustls-tls-webpki-roots"], optional = true }
futures-util = { version = "0.3", optional = true }
simsearch = { version = "0.3", optional = true }

[features]
default = ["nostr"]
nostr = ["dep:tokio-tungstenite", "dep:futures-util", "dep:simsearch"]

[build-dependencies]
uniffi = { version = "0.31", features = ["build"] }
